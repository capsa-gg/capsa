# Capsa Server

## Requirements

* Go >= v1.23
* Make
* Go dependencies installed via `make bootstrap`

_Development for the server can only be run on Linux and MacOS, for development on Windows, use WSL. Running builds on Windows is supported._

## Development and commands

For all commands, run `make`.
