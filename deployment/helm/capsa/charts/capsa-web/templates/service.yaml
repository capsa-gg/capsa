apiVersion: v1
kind: Service
metadata:
  name: capsa-web-service
  labels:
    {{- include "capsa-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capsa-web.labels" . | nindent 4 }}
